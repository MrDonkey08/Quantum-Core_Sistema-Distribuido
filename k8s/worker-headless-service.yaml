apiVersion: v1
kind: Service
metadata:
  name: worker-headless
spec:
  clusterIP: None # headless: DNS resolves to individual pod IPs
  selector:
    app: mandelbrot-worker
  ports:
    - port: 8080
      targetPort: 8080
